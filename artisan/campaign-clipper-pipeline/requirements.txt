# Core render path. Nothing here is optional.\npillow>=10.0.0\npyyaml>=6.0\npython-dotenv>=1.0.0\n\n# Person-aware smart crop. Ultralytics downloads the configured weight on first\n# use unless YOLO_MODEL_PATH points at a local file. The renderer falls back to\n# centre crop if these optional runtime pieces are unavailable.\nultralytics>=8.2.0\nopencv-python-headless>=4.8.0\nnumpy>=1.24.0\n\n# Campaign board automation. Without it, browse/submit are unavailable and the\n# manual paste path is used instead. Needs: python -m playwright install chromium\nplaywright>=1.40.0\n\n# Content folder download. rclone (external binary) is preferred for large\n# folders; gdown needs no configuration.\ngdown>=5.0.0\n\n# YouTube upload.\ngoogle-api-python-client>=2.100.0\ngoogle-auth-oauthlib>=1.1.0\ngoogle-auth-httplib2>=0.1.1\n\n# Copy generation. Optional; template hooks are used when absent.\ngoogle-generativeai>=0.8.0\n