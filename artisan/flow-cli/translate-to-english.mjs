#!/usr/bin/env node
/** Milo Flow CLI English migration. Copyright © 2026 Daada Allan. */
import fs from 'node:fs';
import path from 'node:path';
const root = path.dirname(new URL(import.meta.url).pathname).replace(/^\/[A-Za-z]:/, (m) => m.slice(1));
const files = ['gen.ts','job.ts','media.ts','_shared.ts'];
const replacements = [
 ['用 Google Omni 生成视频（纯文字 / 多参考图，--refs 引用别名 / 路径 / mediaId）','Generate a video with Google Omni (text or multiple references; --refs accepts aliases, paths, or media IDs)'],
 ['生成提示词','Generation prompt'],['视频时长秒数：4 / 6 / 8 / 10','Video length in seconds: 4 / 6 / 8 / 10'],['一次生成的样本数；每多 1 个加一份积分','Number of samples to generate; each extra sample costs another credit charge'],['项目 ID；不传则用 flow project-use 设置的默认值或当前页面','Project ID; defaults to flow project-use or the current page'],['随机种子，默认随机','Random seed; random by default'],['只算积分不真发请求','Calculate credits without submitting'],['跳过 dry-run 确认，直接提交（agent 用）','Skip dry-run confirmation and submit immediately'],['参考图列表，逗号分隔','Reference images, comma-separated'],['参考视频','Reference video'],['状态','Status'],['任务ID(短)','Job ID (short)'],['消耗积分','Credits'],['余额','Balance'],['备注','Notes'],['未指定项目 ID。请先运行 flow project-use 或在 Flow 网页打开一个项目','No project ID. Run flow project-use or open a Flow project in Chrome first'],['无参考素材','No reference media'],['试算模式未提交；','Dry run; nothing submitted;'],['加 --yes 才真正提交；','Add --yes to submit;'],['已进队列','Queued'],['未找到','Not found'],['任务可能还在排队，或不在当前项目里','The job may still be queued or may not belong to the current project'],['查询某个生成任务的当前状态（按任务 ID）','Show the current status of a generation job'],['等待视频生成任务结束（轮询，直到完成 / 失败 / 超时）','Wait for a video generation job to finish'],['下载已生成完成的视频 mp4 到本地','Download a completed MP4'],['列出当前项目的视频生成任务（按时间倒序）','List video generation jobs for the current project'],['最大等待秒数','Maximum wait in seconds'],['轮询间隔秒','Polling interval in seconds'],['保存路径（含 .mp4 文件名）','Output path, including the .mp4 filename'],['返回条数，按时间倒序','Number of results, newest first'],['过滤状态：done(已完成) / failed(失败) / pending(等待中)；不传显示全部','Filter: done, failed, or pending; omit to show all'],['不截断提示词。配合 `-f csv` 可看完整内容','Do not truncate prompts; use with -f csv for full text'],['本地预览：','Preview locally: '],['文件不存在: ','File not found: '],['上传响应缺少 media.name: ','Upload response is missing media.name: '],['上传图片或视频到 Flow（自动 sha256 去重；视频走 chunked resumable）','Upload an image or video to Flow (SHA-256 dedupe; chunked resumable upload for video)'],['本地图片路径','Local image or video path'],['别名','Alias'],['原文件','Original file'],['尺寸','Dimensions'],['处理方式','Upload mode'],['缓存复用','Cache reused'],['新上传','New upload'],['文件锁超时','File lock timed out'],['另一个进程可能卡住了','Another process may be stuck'],['参考素材 token 为空','Reference media token is empty'],['只接受图片','Only images are accepted'],['视频请用 --ref-video','Use --ref-video for videos'],['无法解析参考素材','Unable to resolve reference media'],['不是 UUID、本地文件，也不是当前项目下的别名','not a UUID, local file, or alias in the current project'],['不能为空',' cannot be empty'],['只接受视频文件','Only video files are accepted'],['图片请用 --refs','Use --refs for images'],['无法解析视频','Unable to resolve video'],['上传时间','Uploaded'],['未拿到 access_token；请确保 Chrome 已登录 Flow','Could not read an access token; make sure Chrome is signed in to Flow'],['未能从 page 获取 labs.google cookie；Flow 可能未登录','Could not read labs.google cookies; Flow may not be signed in'],['请用 --projectId 提供完整的项目 UUID','Provide a full project UUID with --projectId'],['项目','project'],['上传','upload'],['下载','download'],['生成','generation'],['等待','waiting'],['提示词','Prompt'],['已完成时下载','Download when complete'],['跟踪','Track'],['余额','Balance'],['服务等级','Service tier']
];
for (const file of files) {
  const target = path.join(root, file);
  let text = fs.readFileSync(target, 'utf8');
  for (const [from, to] of replacements) text = text.split(from).join(to);
  fs.writeFileSync(target, text, 'utf8');
  console.log(`Milo Flow CLI: translated ${file}`);
}
